This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
docs/capturas/.gitkeep
index.html
package.json
public/img/genshin_impact.jpeg
public/img/hogwarts_legacy.webp
public/img/marvel_rivals.jpeg
public/img/minecraft.webp
public/img/overwatch2.webp
public/img/resident_evil_4_remake.webp
public/productos.json
README.md
src/App.jsx
src/components/Alerta.jsx
src/components/Beneficios.jsx
src/components/Carrusel.jsx
src/components/CartItem.jsx
src/components/CartTotal.jsx
src/components/ContactForm.jsx
src/components/Footer.jsx
src/components/Hero.jsx
src/components/Navbar.jsx
src/components/ProductCard.jsx
src/components/ProductList.jsx
src/components/ShoppingCart.jsx
src/hooks/useCarrito.js
src/index.css
src/main.jsx
src/utils/formatearPrecio.js
vite.config.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gitignore">
node_modules
dist
</file>

<file path="docs/capturas/.gitkeep">

</file>

<file path="package.json">
{
  "name": "tienda-videojuegos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://Francisca-Valenzuela.github.io/Tienda_videoJuegos/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": { "bootstrap": "^5.3.3", "react": "^18.3.1", "react-dom": "^18.3.1" },
  "devDependencies": { "@vitejs/plugin-react": "^4.3.1", "gh-pages": "^6.1.1", "vite": "^5.4.0" }
}
</file>

<file path="public/productos.json">
[
  { "id": 1, "titulo": "Minecraft", "descripcion": "Construye, explora y sobrevive en un mundo abierto.", "imagen": "img/minecraft.webp", "precioNormal": 27990, "precioOferta": 22990, "categoria": "Aventura" },
  { "id": 2, "titulo": "Resident Evil 4 Remake", "descripcion": "Survival horror renovado con Leon S. Kennedy.", "imagen": "img/resident_evil_4_remake.webp", "precioNormal": 39990, "precioOferta": 31990, "categoria": "Acción" },
  { "id": 3, "titulo": "Hogwarts Legacy", "descripcion": "Rol en mundo abierto: vive la magia de Hogwarts.", "imagen": "img/hogwarts_legacy.webp", "precioNormal": 34990, "precioOferta": 27990, "categoria": "Aventura" },
  { "id": 4, "titulo": "Overwatch 2", "descripcion": "Acción y disparos en equipo con héroes únicos.", "imagen": "img/overwatch2.webp", "precioNormal": 29990, "precioOferta": 24990, "categoria": "Acción" },
  { "id": 5, "titulo": "Marvel Rivals", "descripcion": "Combates estratégicos con el multiverso de superhéroes.", "imagen": "img/marvel_rivals.jpeg", "precioNormal": 24990, "precioOferta": 19990, "categoria": "Acción" },
  { "id": 6, "titulo": "Genshin Impact", "descripcion": "Explora el mágico mundo de Teyvat en esta aventura de rol.", "imagen": "img/genshin_impact.jpeg", "precioNormal": 19990, "precioOferta": 15990, "categoria": "Aventura" }
]
</file>

<file path="src/App.jsx">
import { useState, useEffect, useMemo, useCallback } from 'react';
import useCarrito from './hooks/useCarrito';
import Navbar from './components/Navbar';
import Alerta from './components/Alerta';
import Hero from './components/Hero';
import Carrusel from './components/Carrusel';
import Beneficios from './components/Beneficios';
import ProductList from './components/ProductList';
import ShoppingCart from './components/ShoppingCart';
import ContactForm from './components/ContactForm';
import Footer from './components/Footer';

/** 
 * Componente raíz: concentra el estado compartido de la tienda 
 * y gestiona el ciclo de vida de la carga de productos. 
 */
export default function App() {
  const [productos, setProductos] = useState([]);
  const [cargando, setCargando] = useState(true);
  const [error, setError] = useState(null);
  const [busqueda, setBusqueda] = useState('');
  const [categoria, setCategoria] = useState('todos');
  const [alerta, setAlerta] = useState(null);
  
  // Custom hook que abstrae la lógica del carrito de compras
  const carro = useCarrito();

  // useEffect: Carga el catálogo asegurando la limpieza (cleanup) con AbortController
  useEffect(() => {
    const controller = new AbortController();
    const signal = controller.signal;

    fetch(`${import.meta.env.BASE_URL}productos.json`, { signal })
      .then((r) => {
        if (!r.ok) throw new Error(`Error de red: ${r.status}`);
        return r.json();
      })
      .then(setProductos)
      .catch((e) => {
        // Ignorar el error si la petición fue abortada intencionalmente al desmontar
        if (e.name !== 'AbortError') {
          console.error('Error al cargar productos:', e);
          setError('No fue posible cargar el catálogo. Por favor, intenta más tarde.');
        }
      })
      .finally(() => setCargando(false));

    return () => controller.abort(); // Limpieza al desmontar el componente
  }, []);

  // useMemo: Filtra productos solo cuando cambian las dependencias relevantes
  const visibles = useMemo(
    () =>
      productos.filter(
        (p) =>
          (categoria === 'todos' || p.categoria === categoria) &&
          p.titulo.toLowerCase().includes(busqueda.trim().toLowerCase())
      ),
    [productos, categoria, busqueda]
  );

  const cerrarAlerta = useCallback(() => setAlerta(null), []);

  // useCallback: Mantiene la misma referencia de la función para optimizar props pasadas a hijos
  const manejarAgregar = useCallback((producto) => {
    carro.agregar(producto);
    setAlerta({ id: Date.now(), tipo: 'success', mensaje: `"${producto.titulo}" se agregó al carrito.` });
  }, [carro]);

  const manejarCategoria = (cat) => {
    setCategoria(cat);
    setBusqueda('');
  };

  const irAProductos = () => document.getElementById('productos')?.scrollIntoView({ behavior: 'smooth' });

  return (
    <div className="d-flex flex-column min-vh-100">
      <Navbar
        cantidad={carro.cantidadTotal}
        categoria={categoria}
        onCategoria={manejarCategoria}
        busqueda={busqueda}
        onBusqueda={setBusqueda}
        onBuscar={irAProductos}
      />
      
      {/* Renderizado condicional de la alerta temporal */}
      {alerta && (
        <div className="bg-alertas-wrapper" aria-live="assertive">
          <div className="container">
            <Alerta key={alerta.id} tipo={alerta.tipo} mensaje={alerta.mensaje} onCerrar={cerrarAlerta} />
          </div>
        </div>
      )}
      
      <Hero />
      <Carrusel />
      
      <main className="container my-5">
        <Beneficios />
        <ProductList 
          productos={visibles} 
          cargando={cargando} 
          error={error} 
          busqueda={busqueda} 
          onAgregar={manejarAgregar} 
        />
        <ShoppingCart
          carrito={carro.carrito}
          cantidadTotal={carro.cantidadTotal}
          total={carro.total}
          onSumar={manejarAgregar} /* Refactorizado: Se pasa la función directamente */
          onRestar={carro.restar}
          onQuitar={carro.quitar}
          onVaciar={carro.vaciar}
        />
        <ContactForm />
      </main>
      <Footer />
    </div>
  );
}
</file>

<file path="src/components/Alerta.jsx">
import { useEffect } from 'react';

/** Alerta de Bootstrap que se cierra sola a los 4 segundos. Props: tipo, mensaje, onCerrar. */
export default function Alerta({ tipo, mensaje, onCerrar }) {
  useEffect(() => {
    const temporizador = setTimeout(onCerrar, 4000);
    return () => clearTimeout(temporizador); // limpieza al desmontar
  }, [onCerrar]);

  return (
    <div className={`alert alert-${tipo} alert-dismissible fade show mb-2 mt-3`} role="alert">
      {mensaje}
      <button type="button" className="btn-close" aria-label="Cerrar" onClick={onCerrar}></button>
    </div>
  );
}
</file>

<file path="src/components/Beneficios.jsx">
const BENEFICIOS = [
  { icono: '🚚', titulo: 'Envío rápido', texto: 'Recibe tus juegos en tiempo récord en tu domicilio.' },
  { icono: '💳', titulo: 'Pago seguro', texto: 'Todas tus transacciones están cifradas y protegidas.' },
  { icono: '🎧', titulo: 'Soporte 24/7', texto: 'Atención personalizada para tus dudas a toda hora.' },
];

/** Sección "¿Por qué comprar con nosotros?" generada a partir de un arreglo. */
export default function Beneficios() {
  return (
    <section id="nosotros" className="mb-5">
      <h2 className="text-center mb-4 fw-bold" style={{ color: 'var(--color-primario)' }}>¿Por qué comprar con nosotros?</h2>
      <div className="row g-4 text-center justify-content-center">
        {BENEFICIOS.map((b) => (
          <div className="col-12 col-sm-6 col-lg-4" key={b.titulo}>
            <div className="p-3 border rounded-3 shadow-sm bg-white h-100">
              <div className="fs-1">{b.icono}</div>
              <h3 className="h5 fw-bold mt-2">{b.titulo}</h3>
              <p className="text-secondary small mb-0">{b.texto}</p>
            </div>
          </div>
        ))}
      </div>
    </section>
  );
}
</file>

<file path="src/components/Carrusel.jsx">
import { useState, useEffect } from 'react';

const BASE = import.meta.env.BASE_URL;
const SLIDES = [
  { img: 'minecraft.webp', titulo: 'Minecraft', texto: 'Construye, explora y sobrevive en un mundo abierto de bloques.' },
  { img: 'resident_evil_4_remake.webp', titulo: 'Resident Evil 4 Remake', texto: 'Sobrevive al horror en la piel de Leon S. Kennedy.' },
  { img: 'hogwarts_legacy.webp', titulo: 'Hogwarts Legacy', texto: 'Vive la experiencia de estudiar magia en Hogwarts.' },
];

/** Carrusel de destacados controlado con useState y useEffect (cambia cada 3 s). */
export default function Carrusel() {
  const [actual, setActual] = useState(0);
  const ir = (i) => setActual((i + SLIDES.length) % SLIDES.length);

  useEffect(() => {
    const intervalo = setInterval(() => setActual((a) => (a + 1) % SLIDES.length), 3000);
    return () => clearInterval(intervalo); // se reinicia al cambiar de slide manualmente
  }, [actual]);

  return (
    <section aria-label="Destacados">
      <div className="carousel slide">
        <div className="carousel-indicators">
          {SLIDES.map((s, i) => (
            <button key={s.img} type="button" className={i === actual ? 'active' : ''} aria-label={`Slide ${i + 1}`} onClick={() => ir(i)}></button>
          ))}
        </div>
        <div className="carousel-inner">
          {SLIDES.map((s, i) => (
            <div key={s.img} className={`carousel-item ${i === actual ? 'active' : ''}`}>
              <img src={`${BASE}img/${s.img}`} className="d-block w-100 object-fit-cover carousel-img" alt={`Carátula del juego ${s.titulo}`} />
              <div className="carousel-caption">
                <h5 className="fw-bold bg-dark bg-opacity-50 d-inline-block p-2 rounded">{s.titulo}</h5>
                <p className="d-none d-md-block">{s.texto}</p>
              </div>
            </div>
          ))}
        </div>
        <button className="carousel-control-prev" type="button" onClick={() => ir(actual - 1)}>
          <span className="carousel-control-prev-icon" aria-hidden="true"></span>
          <span className="visually-hidden">Anterior</span>
        </button>
        <button className="carousel-control-next" type="button" onClick={() => ir(actual + 1)}>
          <span className="carousel-control-next-icon" aria-hidden="true"></span>
          <span className="visually-hidden">Siguiente</span>
        </button>
      </div>
    </section>
  );
}
</file>

<file path="src/components/CartItem.jsx">
import { formatearPrecio } from '../utils/formatearPrecio';

/** Una línea del carrito con botones − / + y eliminar. */
export default function CartItem({ item, onSumar, onRestar, onQuitar }) {
  return (
    <li className="list-group-item d-flex justify-content-between align-items-center px-0 bg-transparent">
      <span className="small">{item.titulo} <span className="text-secondary">x{item.cantidad}</span></span>
      <span className="d-flex align-items-center gap-2">
        <strong className="small">{formatearPrecio(item.precioOferta * item.cantidad)}</strong>
        <button type="button" className="btn btn-sm btn-outline-secondary py-0 px-2" aria-label={`Restar una unidad de ${item.titulo}`} onClick={() => onRestar(item.id)}>−</button>
        <button type="button" className="btn btn-sm btn-outline-secondary py-0 px-2" aria-label={`Sumar una unidad de ${item.titulo}`} onClick={() => onSumar(item.id)}>+</button>
        <button type="button" className="btn btn-sm btn-outline-danger py-0 px-2" aria-label={`Quitar ${item.titulo} del carrito`} onClick={() => onQuitar(item.id)}>✕</button>
      </span>
    </li>
  );
}
</file>

<file path="src/components/CartTotal.jsx">
import { formatearPrecio } from '../utils/formatearPrecio';

/** Muestra el total a pagar. Props: total. */
export default function CartTotal({ total }) {
  return (
    <div className="d-flex justify-content-between align-items-center border-top pt-3">
      <strong style={{ color: 'var(--color-secundario)' }}>Total:</strong>
      <strong style={{ color: 'var(--color-primario)' }}>{formatearPrecio(total)}</strong>
    </div>
  );
}
</file>

<file path="src/components/ContactForm.jsx">
import { useState } from 'react';

const REGEX_CORREO = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const INICIAL = { nombre: '', email: '', mensaje: '' };

/** 
 * Formulario de contacto con inputs controlados (onChange) y validación al enviar (onSubmit). 
 * Se ha mejorado la accesibilidad mediante el uso de atributos ARIA.
 */
export default function ContactForm() {
  const [datos, setDatos] = useState(INICIAL);
  const [aviso, setAviso] = useState(null); // Estado para renderizado condicional de la alerta: { tipo, texto }

  // Handler dinámico para actualizar el estado basado en el atributo 'name' del input
  const manejarCambio = (e) => setDatos({ ...datos, [e.target.name]: e.target.value });

  const manejarSubmit = (e) => {
    e.preventDefault();
    const { nombre, email, mensaje } = datos;
    
    // Validación de campos vacíos
    if (!nombre.trim() || !email.trim() || !mensaje.trim()) {
      return setAviso({ tipo: 'danger', texto: 'Por favor, completa todos los campos obligatorios del formulario antes de enviar.' });
    }
    
    // Validación mediante expresión regular para el correo
    if (!REGEX_CORREO.test(email.trim())) {
      return setAviso({ tipo: 'danger', texto: 'Por favor, ingresa un correo electrónico válido que contenga un "@" y un dominio.' });
    }
    
    // Éxito
    setAviso({ tipo: 'success', texto: `Gracias por contactarnos, ${nombre.trim()}. Hemos recibido tu mensaje y te responderemos a ${email.trim()}.` });
    setDatos(INICIAL);
  };

  const etiqueta = { color: 'var(--color-secundario)' };

  return (
    <section id="contacto" className="p-4 rounded-4 shadow-sm bg-white border" style={{ borderColor: '#eee' }}>
      <div className="row justify-content-center">
        <div className="col-12 col-md-8 col-lg-6">
          <div className="text-center mb-4">
            <h2 className="h4 mb-3 fw-bold" style={{ color: 'var(--color-primario)' }}>Contacto</h2>
            <p className="text-secondary">
              ¿Tienes dudas sobre nuestros productos? Escríbenos a{' '}
              <a href="mailto:soporte@videojuegos.cl" className="text-decoration-none fw-bold text-dark">soporte@videojuegos.cl</a>{' '}
              o completa el siguiente formulario:
            </p>
          </div>
          
          <form onSubmit={manejarSubmit} noValidate>
            
            {/* Renderizado condicional de la alerta. aria-live="assertive" asegura que los lectores de pantalla lo anuncien de inmediato */}
            {aviso && (
              <div 
                className={`alert alert-${aviso.tipo} alert-dismissible fade show mb-4`} 
                role="alert"
                aria-live="assertive" 
              >
                <strong>{aviso.tipo === 'danger' ? '¡Error!' : '¡Éxito!'}</strong> {aviso.texto}
                <button type="button" className="btn-close" aria-label="Cerrar" onClick={() => setAviso(null)}></button>
              </div>
            )}
            
            <div className="mb-3">
              <label htmlFor="nombre" className="form-label fw-bold" style={etiqueta}>Nombre: <span className="text-danger" aria-hidden="true">*</span></label>
              <input type="text" className="form-control" id="nombre" name="nombre" placeholder="Ingresa tu nombre" value={datos.nombre} onChange={manejarCambio} aria-required="true" />
            </div>
            
            <div className="mb-3">
              <label htmlFor="email" className="form-label fw-bold" style={etiqueta}>Correo electrónico: <span className="text-danger" aria-hidden="true">*</span></label>
              <input type="email" className="form-control" id="email" name="email" placeholder="ejemplo@correo.com" value={datos.email} onChange={manejarCambio} aria-required="true" />
            </div>
            
            <div className="mb-4">
              <label htmlFor="mensaje" className="form-label fw-bold" style={etiqueta}>Mensaje: <span className="text-danger" aria-hidden="true">*</span></label>
              <textarea className="form-control" id="mensaje" name="mensaje" rows="4" placeholder="Escribe tu mensaje aquí..." value={datos.mensaje} onChange={manejarCambio} aria-required="true"></textarea>
            </div>
            
            <div className="text-center">
              <button type="submit" className="btn btn-custom fw-bold px-5 py-2">Enviar mensaje</button>
            </div>
          </form>
          
        </div>
      </div>
    </section>
  );
}
</file>

<file path="src/components/Footer.jsx">
/** Pie de página. */
export default function Footer() {
  return (
    <footer className="text-center py-4 mt-auto footer-bg">
      <div className="container text-white">
        <p className="fw-bold mb-1">© 2026 Videojuegos. Todos los derechos reservados.</p>
        <p className="mb-1 text-light opacity-75 small">Calle Wallaby 42, Sídney</p>
        <p className="mb-1 text-light opacity-75 small">soporte@videojuegos.cl</p>
        <p className="mb-0 text-light opacity-75 small">
          <a href="#" className="text-light text-decoration-none mx-1">📷 Instagram</a> ·{' '}
          <a href="#" className="text-light text-decoration-none mx-1">🐦 X</a> ·{' '}
          <a href="#" className="text-light text-decoration-none mx-1">🎮 Discord</a>
        </p>
      </div>
    </footer>
  );
}
</file>

<file path="src/components/Hero.jsx">
/** Encabezado principal de la página. */
export default function Hero() {
  return (
    <header id="inicio" className="text-center text-white py-5 header-bg">
      <div className="container">
        <h1 className="display-5 fw-bold">Videojuegos</h1>
        <p className="lead">Bienvenido a nuestra página de videojuegos</p>
      </div>
    </header>
  );
}
</file>

<file path="src/components/Navbar.jsx">
import { useState } from 'react';

const CATEGORIAS = [
  { id: 'todos', texto: 'Productos' },
  { id: 'Acción', texto: 'Acción' },
  { id: 'Aventura', texto: 'Aventura' },
];

/** Barra de navegación con categorías, buscador y contador del carrito. */
export default function Navbar({ cantidad, categoria, onCategoria, busqueda, onBusqueda, onBuscar }) {
  const [abierto, setAbierto] = useState(false); // menú colapsable en móvil

  const manejarSubmit = (e) => {
    e.preventDefault();
    onBuscar();
  };

  return (
    <nav className="navbar navbar-expand-lg navbar-dark sticky-top" style={{ backgroundColor: 'var(--color-secundario)' }}>
      <div className="container-fluid px-4">
        <a className="navbar-brand fw-bold fs-3" href="#inicio">🎮 Videojuegos</a>
        <button className="navbar-toggler" type="button" aria-expanded={abierto} aria-label="Abrir menú de navegación" onClick={() => setAbierto(!abierto)}>
          <span className="navbar-toggler-icon"></span>
        </button>
        <div className={`collapse navbar-collapse justify-content-end ${abierto ? 'show' : ''}`}>
          <ul className="navbar-nav gap-2 me-auto ms-lg-3">
            <li className="nav-item"><a className="nav-link" href="#inicio">Inicio</a></li>
            <li className="nav-item"><a className="nav-link" href="#nosotros">Nosotros</a></li>
            {CATEGORIAS.map((c) => (
              <li className="nav-item" key={c.id}>
                <a className={`nav-link ${categoria === c.id ? 'active' : ''}`} href="#productos" onClick={() => onCategoria(c.id)}>
                  {c.texto}
                </a>
              </li>
            ))}
            <li className="nav-item"><a className="nav-link" href="#contacto">Contacto</a></li>
          </ul>

          <form id="formulario-busqueda" className="d-flex my-2 my-lg-0 me-lg-3" role="search" onSubmit={manejarSubmit}>
            <label htmlFor="input-busqueda" className="visually-hidden">Buscar juego</label>
            <input className="form-control form-control-sm" type="search" id="input-busqueda" placeholder="Buscar juego..." value={busqueda} onChange={(e) => onBusqueda(e.target.value)} />
            <button className="btn btn-sm btn-custom ms-2" type="submit" aria-label="Buscar">🔍</button>
          </form>

          <a href="#carrito" className="btn btn-sm btn-custom position-relative align-self-start align-self-lg-center" title="Ver carrito">
            🛒 Carrito
            <span className="badge rounded-pill bg-danger position-absolute top-0 start-100 translate-middle" id="contador-carrito">{cantidad}</span>
          </a>
        </div>
      </div>
    </nav>
  );
}
</file>

<file path="src/components/ProductCard.jsx">
import { formatearPrecio } from '../utils/formatearPrecio';

/** Tarjeta de un producto. Props: producto, onAgregar. */
export default function ProductCard({ producto, onAgregar }) {
  const { titulo, descripcion, imagen, precioNormal, precioOferta, categoria } = producto;
  const descuento = Math.round((1 - precioOferta / precioNormal) * 100);

  return (
    <div className="col-12 col-sm-6 col-lg-4">
      <div className="card h-100 shadow-sm producto-card border-0">
        <img src={`${import.meta.env.BASE_URL}${imagen}`} className="card-img-top" alt={`Carátula de ${titulo}`} loading="lazy" />
        <div className="card-body d-flex flex-column bg-light rounded-bottom">
          <div className="d-flex justify-content-between align-items-start mb-2">
            <span className="badge bg-secondary">{categoria}</span>
            <span className="badge bg-danger">-{descuento}%</span>
          </div>
          <h3 className="card-title h5 fw-bold" style={{ color: 'var(--color-secundario)' }}>{titulo}</h3>
          <p className="card-text text-secondary small">{descripcion}</p>
          <p className="mb-2">
            <s className="text-secondary small me-2">{formatearPrecio(precioNormal)}</s>
            <span className="fw-bold fs-5" style={{ color: 'var(--color-primario)' }}>{formatearPrecio(precioOferta)}</span>
          </p>
          <button type="button" className="btn btn-custom mt-auto fw-bold" onClick={() => onAgregar(producto)}>
            Agregar al carrito 🛒
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/components/ProductList.jsx">
import ProductCard from './ProductCard';

/** Listado de productos con renderizado condicional: cargando, error, sin resultados o lista. */
export default function ProductList({ productos, cargando, error, busqueda, onAgregar }) {
  let contenido;
  if (cargando) {
    contenido = <div className="text-center"><div className="spinner-border" role="status"><span className="visually-hidden">Cargando...</span></div></div>;
  } else if (error) {
    contenido = <div className="alert alert-danger" role="alert">{error}</div>;
  } else if (productos.length === 0) {
    contenido = (
      <p className="text-secondary text-center">
        {busqueda ? <>No se encontraron juegos que coincidan con "<strong>{busqueda}</strong>".</> : 'No hay juegos en esta categoría.'}
      </p>
    );
  } else {
    contenido = (
      <div className="row g-4 justify-content-center">
        {productos.map((p) => <ProductCard key={p.id} producto={p} onAgregar={onAgregar} />)}
      </div>
    );
  }

  return (
    <section id="productos" className="mb-5">
      <h2 className="text-center mb-4 fw-bold" style={{ color: 'var(--color-primario)' }}>Productos Destacados</h2>
      {contenido}
    </section>
  );
}
</file>

<file path="src/components/ShoppingCart.jsx">
import CartItem from './CartItem';
import CartTotal from './CartTotal';

/** 
 * Resumen del carrito: Renderizado condicional que muestra un mensaje si está vacío 
 * o renderiza la lista de productos, el contador de unidades y el cálculo del total. 
 */
export default function ShoppingCart({ carrito, cantidadTotal, total, onSumar, onRestar, onQuitar, onVaciar }) {
  return (
    <section id="carrito" className="mb-5">
      <h2 className="text-center mb-4 fw-bold" style={{ color: 'var(--color-primario)' }}>
        🛒 Tu Carrito
      </h2>
      <div className="row justify-content-center">
        <div className="col-12 col-md-8 col-lg-6">
          <div className="p-4 rounded-4 shadow-sm bg-white border" style={{ borderColor: '#eee' }}>
            
            {/* Renderizado condicional dependiendo del contenido del carrito */}
            {carrito.length === 0 ? (
              <p className="text-secondary text-center mb-0" aria-live="polite">
                Tu carrito está vacío. ¡Agrega algún juego!
              </p>
            ) : (
              <>
                <p className="small text-secondary mb-2" aria-live="polite">
                  Productos en el carrito: <strong>{cantidadTotal}</strong>
                </p>
                
                <ul id="lista-carrito" className="list-group list-group-flush mb-3">
                  {carrito.map((item) => (
                    <CartItem 
                      key={item.id} 
                      item={item} 
                      // Se mapea la función para entregar el objeto producto completo y evitar el .find() en App.jsx
                      onSumar={() => onSumar(item)} 
                      onRestar={onRestar} 
                      onQuitar={onQuitar} 
                    />
                  ))}
                </ul>
                
                <CartTotal total={total} />
                
                <div className="text-center mt-3">
                  <button 
                    type="button" 
                    className="btn btn-outline-secondary btn-sm" 
                    onClick={onVaciar}
                  >
                    Vaciar carrito
                  </button>
                </div>
              </>
            )}

          </div>
        </div>
      </div>
    </section>
  );
}
</file>

<file path="src/hooks/useCarrito.js">
import { useState } from 'react';

/**
 * Hook personalizado con toda la lógica del carrito (estado con useState).
 * Devuelve el carrito, las acciones y los valores calculados (cantidad y total).
 */
export default function useCarrito() {
  const [carrito, setCarrito] = useState([]);

  // Agrega un producto o suma 1 a su cantidad si ya existe
  const agregar = (producto) =>
    setCarrito((prev) =>
      prev.some((i) => i.id === producto.id)
        ? prev.map((i) => (i.id === producto.id ? { ...i, cantidad: i.cantidad + 1 } : i))
        : [...prev, { ...producto, cantidad: 1 }]
    );

  // Resta 1 unidad; si llega a 0 el producto se elimina
  const restar = (id) =>
    setCarrito((prev) => prev.map((i) => (i.id === id ? { ...i, cantidad: i.cantidad - 1 } : i)).filter((i) => i.cantidad > 0));

  const quitar = (id) => setCarrito((prev) => prev.filter((i) => i.id !== id));
  const vaciar = () => setCarrito([]);

  // Valores derivados: no necesitan estado propio
  const cantidadTotal = carrito.reduce((acc, i) => acc + i.cantidad, 0);
  const total = carrito.reduce((acc, i) => acc + i.precioOferta * i.cantidad, 0);

  return { carrito, agregar, restar, quitar, vaciar, cantidadTotal, total };
}
</file>

<file path="src/index.css">
/* Variables CSS Globales */
:root {
    --color-primario: #1a1a2e;
    --color-secundario: #16213e;
    --color-acento: #fce205;
    --color-texto: #f1f1f1;
    --fuente-principal: 'Segoe UI', Arial, sans-serif;
}

/* --------------------------------------
   Comportamiento de Scroll Global
--------------------------------------- */
html {
    scroll-padding-top: 85px;
    scroll-behavior: smooth;
}

body {
    font-family: var(--fuente-principal);
    background-color: #fafafa;
    
}

/* --------------------------------------
   Accesibilidad (Navegación por Teclado)
--------------------------------------- */
.nav-link:focus-visible,
.btn-custom:focus-visible,
.navbar-brand:focus-visible,
a:focus-visible {
    outline: 3px solid var(--color-acento) !important;
    outline-offset: 4px;
    border-radius: 4px;
}

/* --------------------------------------
   Navbar y Navegación
--------------------------------------- */
.nav-link {
    transition: color 0.2s ease, border-bottom 0.2s ease;
    border-bottom: 2px solid transparent;
    color: var(--color-texto) !important;
    font-weight: 600;
}

.nav-link:hover,
.nav-link.active {
    color: var(--color-acento) !important;
    border-bottom: 2px solid var(--color-acento);
}

/* Formulario de búsqueda en la navbar */
#formulario-busqueda .form-control {
    min-width: 140px;
}

/* --------------------------------------
   Header Hero y Footer
--------------------------------------- */
.header-bg {
    background-color: var(--color-primario);
    border-bottom: 4px solid var(--color-acento);
}

.footer-bg {
    background-color: var(--color-primario);
    border-top: 4px solid var(--color-acento);
}

/* --------------------------------------
   Carrusel
--------------------------------------- */
.carousel-img {
    filter: brightness(0.75);
}

.carousel-caption h5 {
    font-weight: bold;
}

/* --------------------------------------
   Tarjetas (Cards)
--------------------------------------- */
.card-img-top {
    height: 220px;
    object-fit: cover;
    border-bottom: 3px solid var(--color-secundario);
}

.producto-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.producto-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15) !important;
    border-bottom: 3px solid var(--color-acento) !important;
}

/* Botón personalizado */
.btn-custom {
    background-color: var(--color-secundario);
    border: 1px solid var(--color-secundario);
    color: var(--color-texto);
    transition: all 0.3s ease;
}

.btn-custom:hover {
    background-color: var(--color-primario);
    border-color: var(--color-acento);
    color: var(--color-acento);
}

/* --------------------------------------
   Carrito de Compras
--------------------------------------- */
#lista-carrito .list-group-item {
    border-color: #eee;
}

#contador-carrito {
    font-size: 0.65rem;
}

/* --------------------------------------
  Modelo de Cajas y Flexbox
--------------------------------------- */
body, html {
    margin: 0 !important;
    padding: 0;
}

/* --------------------------------------
   Contenedor de Alertas Globales
--------------------------------------- */
.bg-alertas-wrapper {
    background-color: var(--color-primario); /* Adoptará el tono oscuro de tu header */
}

/* --------------------------------------
   Semana 7: precios de oferta y hover (antes hecho con JS)
--------------------------------------- */
.producto-card:hover { opacity: 0.9; }
</file>

<file path="src/main.jsx">
import React from 'react';
import ReactDOM from 'react-dom/client';
import 'bootstrap/dist/css/bootstrap.min.css';
import './index.css';
import App from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
</file>

<file path="src/utils/formatearPrecio.js">
// Convierte un número a formato de peso chileno: 27990 -> "$27.990"
export const formatearPrecio = (valor) => `$${valor.toLocaleString('es-CL')}`;
</file>

<file path="vite.config.js">
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// "base" debe coincidir con el nombre del repositorio para que funcione en GitHub Pages
export default defineConfig({ plugins: [react()], base: '/Tienda_videoJuegos/' });
</file>

<file path="README.md">
# 🎮 Tienda de Videojuegos – React (Semana 7)

Tienda de videojuegos interactiva desarrollada para **Desarrollo Frontend I (PFY2201)**. Evolución del proyecto de las semanas 5 y 6 (HTML + JS) migrada a **React + Vite**, manteniendo el diseño original con Bootstrap 5.

- 🔗 **Sitio publicado:** https://Francisca-Valenzuela.github.io/Tienda_videoJuegos/
- 📦 **Repositorio:** https://github.com/Francisca-Valenzuela/Tienda_videoJuegos

## Funcionalidades
- **Listado de productos** con nombre, precio normal (tachado), precio oferta, descuento, descripción corta e imagen.
- **Carrito de compras:** agregar, sumar/restar unidades, eliminar un producto y vaciar; **contador** de productos y **total** en pesos.
- **Búsqueda** en vivo y **filtro por categoría** (Acción / Aventura).
- **Renderizado condicional:** carrito vacío, estado de carga, error de carga, "sin resultados" y alertas temporales.
- **Formulario de contacto** con validación.

## Conceptos de React aplicados
| Concepto | Dónde |
|---|---|
| Componentes funcionales y props | `src/components/*` |
| `useState` | `useCarrito`, `App`, `Navbar`, `ContactForm`, `Carrusel` |
| `useEffect` | `App` (fetch), `Alerta` (temporizador), `Carrusel` (intervalo) |
| `useMemo` / `useCallback` | `App` (filtrado y cierre de alertas) |
| Hook personalizado | `src/hooks/useCarrito.js` |
| Eventos | `onClick`, `onChange`, `onSubmit` |
| Renderizado condicional | `ProductList`, `ShoppingCart`, `Alerta` |

## Estructura
```
src/
├── components/   Navbar, Hero, Carrusel, Beneficios, ProductList, ProductCard,
│                 ShoppingCart, CartItem, CartTotal, ContactForm, Footer, Alerta
├── hooks/        useCarrito.js
├── utils/        formatearPrecio.js
├── App.jsx  main.jsx  index.css
public/           productos.json e imágenes
```

## Cómo ejecutarlo
```bash
npm install
npm run dev        # http://localhost:5173/Tienda_videoJuegos/
npm run build
npm run deploy     # publica en la rama gh-pages
```

## Capturas de pantalla
> Guarda las imágenes en `docs/capturas/` con estos nombres.

| Funcionalidad | Captura |
|---|---|
| Listado de productos (precio normal y oferta) | ![Listado](docs/capturas/01-listado.png) |
| Agregar al carrito y alerta | ![Agregar](docs/capturas/02-agregar.png) |
| Carrito con contador y total | ![Carrito](docs/capturas/03-carrito.png) |
| Eliminar / restar producto | ![Eliminar](docs/capturas/04-eliminar.png) |
| Carrito vacío | ![Vacío](docs/capturas/05-vacio.png) |
| Búsqueda sin resultados | ![Búsqueda](docs/capturas/06-busqueda.png) |
| Filtro por categoría | ![Categoría](docs/capturas/07-categoria.png) |
| Formulario de contacto | ![Contacto](docs/capturas/08-contacto.png) |
</file>

<file path="index.html">
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>🎮 Videojuegos</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
</file>

</files>
